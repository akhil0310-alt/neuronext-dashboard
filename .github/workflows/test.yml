name: Test dispatch 6
on:
  workflow_dispatch: {}
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: "pip install --only-binary=:all: gspread google-auth"
